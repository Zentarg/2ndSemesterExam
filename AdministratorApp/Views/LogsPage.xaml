<Page
    x:Class="AdministratorApp.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdministratorApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:dc="using:AdministratorApp.ViewModels">

    <Page.DataContext>
        <dc:LogsPageVM/>
    </Page.DataContext>

    <Grid>
        <ListView ItemsSource="{Binding LogEntries}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dc:LogsPageVM">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="10,0,0,0" Text="["/>
                        <TextBlock Margin="0,0,0,0" Text="{Binding RequestType}" Visibility="Visible"/>
                        <TextBlock Margin="0,0,0,0" Text="]"/>
                        <TextBlock Margin="10,0,0,0" Text="{Binding LogEntry}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
